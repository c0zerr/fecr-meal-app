Hatanın %87 civarında takılması ve `Process completed with exit code 1` demesi, genellikle **Java Heap Space (RAM) yetersizliği** veya **R8 (Code Shrinker) hatasından** kaynaklanır.

GitHub Actions'ın ücretsiz makineleri bazen büyük projelerde bellek sınırına takılabiliyor.

### Çözüm Planı:
1.  **`gradle.properties` Dosyasını Güncelleme:**
    *   Gradle'ın kullanabileceği belleği (RAM) artıracağım (`-Xmx2048m` veya daha fazlası).
    *   AndroidX kullanımını ve Jetifier'ı netleştireceğim.
    *   **En önemlisi:** R8 küçültme işlemini (minifyEnabled) geçici olarak devre dışı bırakmayı veya kurallarını gevşetmeyi deneyeceğiz. Ancak önce RAM artırımı deneyeceğim.

2.  **`android/app/build.gradle` Güncellemesi:**
    *   `release` build tipinde `minifyEnabled true` ise bunu `false` yapıp bir deneme yapacağız. Eğer `false` ise ve hala hata veriyorsa, sorun bellektir.
    *   (Dosyayı okuyup durumuna bakacağım).

3.  **GitHub Actions Workflow Güncellemesi:**
    *   Workflow dosyasına Java heap boyutunu artıran bir parametre ekleyebilirim.

Öncelikle `gradle.properties` ve `build.gradle` dosyalarını kontrol edip RAM artırımı yapalım mı? Bu genellikle %87'de takılma sorununu çözer.