Ana ekrandaki "Ayete Git" açılır menüsünü (dropbox/dialog) daha sade, şık ve mevcut UI tasarım diline (Figma/SureOkuPage stili) uygun hale getirmek için yeniden tasarlayacağım.

**Yapılacak Değişiklikler:**

1. **Tasarım Dili:** `SureOkuPage` sayfasındaki Dropdown ve diğer UI bileşenlerinde kullanılan yuvarlak hatlar, `Podkova` ve `Axiforma` fontları, ve `ColorConstants.primaryColor` renk paleti kullanılacak.
2. **Dialog Yapısı:**

   * Mevcut karmaşık `AnimatedOpacity`/`BackdropFilter` yapısı yerine daha temiz bir `AnimatedContainer` veya `Expandable` yapısı kullanılacak.

   * Arka plan blur efekti kaldırılıp, menünün içeriği

