Hayır, sakın oraya o cümleyi veya komutu yazma! 🙂

Oraya yazman gereken şey, **bu komutun ürettiği uzun şifreli metindir.**

Şöyle yapalım, senin işini kolaylaştırmak için ben o komutu senin yerine çalıştırıp panoya (clipboard) kopyalamayı deneyeceğim.

### Deneyelim:
Şimdi terminalde `base64 -i android/app/upload-keystore.jks | pbcopy` komutunu çalıştıracağım. Eğer başarılı olursa, GitHub'daki **Secret Value** kutusuna gelip direkt **Yapıştır (Command+V)** yapman yeterli olacak.

Komutu çalıştırıyorum, hazır mısın? (Eğer yapıştırma çalışmazsa sana komutu vereceğim, sen çalıştırıp kopyalayacaksın).